.popover {
    position: absolute;
    color: #fff;
    background: rgba(0, 0, 0, .9);
    line-height:1.1rem;
    top: -1.4rem;
    text-align: center;
    font-size: .65rem;
    border-radius: .2rem;
    white-space: nowrap;
    padding: 0 .25rem;
    left: 0;
    display:none;
    &.active{
        display:block;
    }
    &:after {
        border: 4px solid transparent;
        border-top: 4px solid #000;
        opacity: .9;
        width: 0;
        height: 0;
        position: absolute;
        bottom: -0.4rem;
        left: .4rem;
        content: '';
        margin-left: 0;
    }
}
